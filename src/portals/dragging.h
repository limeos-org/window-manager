#pragma once
#include "../all.h"

/**
 * Checks whether a portal drag operation is currently in progress.
 *
 * @return - `True (1)` A drag operation is in progress.
 * @return - `False (0)` No drag operation is in progress.
 */
bool is_portal_dragging();
